#include "main.h"
/**
 * _isupper - check if uppercase letters
 * @c: char to check
 *
 * Return: if uppercase return 1, if not return 0
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= 'z')

	return (0);
}
